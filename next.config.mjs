/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { hostname: "1wwqqqej85fshazq.public.blob.vercel-storage.com" },
    ],
  },
};

export default nextConfig;
